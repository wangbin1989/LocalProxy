# v0.0.1 — run 命令

## 概述

首次迭代，实现 `run` 子命令，用于快速启动单个端口代理。接收本地端口、远程地址、远程端口和协议类型作为参数，输出配置确认后退出。

## 命令规格

```
localproxy run -l <port> -H <host> -p <port> -P <tcp|udp|http>
```

### 选项

| 选项 | 别名 | 类型 | 必填 | 说明 |
|---|---|---|---|---|
| `--local-port` | `-l` | `int` (0–65535) | 是 | 本地监听端口 |
| `--remote-host` | `-H` | `string` | 是 | 远程目标主机地址（IP 或域名） |
| `--remote-port` | `-p` | `int` (0–65535) | 是 | 远程目标端口 |
| `--protocol` | `-P` | `tcp \| udp \| http` | 是 | 代理协议类型 |

### 行为流程

1. 解析命令行参数
2. 校验：端口范围、协议值、主机地址非空
3. 通过 Spectre.Console 面板输出代理配置摘要
4. 退出，状态码 0（成功）或 1（校验失败）

### 输出示例

```
╭────────────────────────────────────────────────╮
│  Proxy Started                                  │
├────────────────────────────────────────────────┤
│  Local         :8080                           │
│  Remote        example.com:80                  │
│  Protocol      TCP                              │
╰────────────────────────────────────────────────╯
```

## 全局选项

| 选项 | 说明 |
|---|---|
| `--help` / `-h` | 显示帮助信息 |
| `--version` | 输出版本号 |


## 退出码

| 码 | 含义 |
|---|---|
| 0 | 成功 |
| 1 | 参数校验失败 |

## 数据模型

```csharp
enum ProxyProtocol { Tcp, Udp, Http }
```

## 源代码结构（规划）

```
src/LocalProxy/
├── Program.cs
├── Commands/
│   └── RunCommand.cs
├── Handlers/
│   └── RunHandler.cs
├── Models/
│   └── ProxyProtocol.cs
└── Infrastructure/
    └── ConsoleOutput.cs
```

## 技术栈

| 组件 | 版本 | 用途 |
|---|---|---|
| .NET | 10.0 | 运行时，启用 Native AOT |
| System.CommandLine | 2.0.9 | CLI 参数解析 |
| Spectre.Console | 0.57.0 | 终端面板输出 |

